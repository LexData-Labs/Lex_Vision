# =============================================================================
# Lex Vision - Environment Configuration
# =============================================================================

# COMPUTE SETTINGS
# Set to 1 to force CPU-only mode (disable GPU), set to 0 to use GPU if available
FORCE_CPU=1

# BACKEND SERVER SETTINGS
# Port for the backend API server
PORT=8000

# Enable auto-reload for development (0=disabled, 1=enabled)
UVICORN_RELOAD=0

# CAMERA SETTINGS
# Camera device index (usually 0 for default camera, try 1, 2, etc. if not working)
CAMERA_INDEX=0

# Alternative: Use RTSP stream or video file path instead of camera
# CAMERA_SOURCE=rtsp://192.168.1.100:554/stream
# CAMERA_SOURCE=C:/path/to/video.mp4

# DETECTION SETTINGS
# Confidence threshold for person detection (0.0 to 1.0, lower = more sensitive)
DETECT_CONF_THRESHOLD=0.3

# CORS SETTINGS
# Comma-separated list of allowed origins for CORS (* allows all)
CORS_ORIGINS=*,http://localhost:5173,http://localhost:8081
